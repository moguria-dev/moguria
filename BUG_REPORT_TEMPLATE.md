# Bug report（互換ポインタ）

現在の受付テンプレートは `.github/ISSUE_TEMPLATE/bug_report.yml` です。

再現時は、commitまたは公開版、OS・端末・ブラウザ、再現手順、期待値と実際、再現率、画面・Wave・直前の選択、console、Service Worker状態を記録してください。修正確認は `docs/TESTING.md` に従います。

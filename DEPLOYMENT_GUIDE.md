# Moguria Deployment Guide（互換ポインタ）

このファイルの旧レンタルサーバー／ZIP手順は廃止しました。現在の公開経路、権限ゲート、確認項目、rollbackは `docs/DEPLOYMENT.md` と `docs/RECOVERY.md` を正本とします。

公開状態は `config/project-state.json` と実際のGitHub Pages設定の両方で確認してください。Service Workerは現在OFFであり、版更新を理由に有効化・変更しません。

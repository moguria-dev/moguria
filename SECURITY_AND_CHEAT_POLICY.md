# Moguria セキュリティ・チート対策方針

## 前提

Moguriaは現時点ではログイン不要・ブラウザ単体・LocalStorage保存の静的ゲームです。
この構成では、ユーザー端末内のデータやJavaScriptは最終的にユーザーが改変可能です。

そのため、完全なチート防止ではなく、以下を目的にします。

- 通常プレイヤーに開発機能が露出しないようにする
- セーブ破損時に復旧・調査しやすくする
- 将来オンラインランキング等を始める時にサーバー検証へ移行しやすくする

## 現在入れている対策

- 開発メニューは `localhost` / `127.0.0.1` かつ `#dev` または `?dev=1` の時だけ表示します。
- 公開GitHub Pagesでは `#dev` / `?dev=1` を付けても開発メニューは表示しません。
- セーブデータはバージョン管理し、破損時は `moguria.corrupt.*` に退避します。
- セーブ保存失敗時にゲーム全体が落ちないよう、保存結果を返します。
- 実績やリザルトには簡易署名を付けられる構造を用意します。これは改ざん防止ではなく、破損検知・デバッグ用です。
- 公開時はHTTPS推奨です。

## 将来ランキング・共有を入れる場合

- ハイスコアや称号判定はサーバー側で再計算します。
- クライアントから送られた数値をそのまま信用しません。
- ランID、プレイ開始時刻、取得スキル、アーティファクト、Wave推移、乱数seedを送信し、サーバー側で妥当性チェックをします。
- 不自然なDPS、移動速度、取得経験値、Waveクリア時間は弾きます。

## 公開時チェック

- 公開URLで `#dev` / `?dev=1` を付けてもDEVボタンが表示されないこと。
- HTTPSで配信されていること。
- Service Workerを有効化する場合、`CACHE_NAME` を更新していること。
- 不要なテスト素材やテスト文言が残っていないこと。
